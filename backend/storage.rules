rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /media/articles/{userId}/{allPaths=**} {
      allow read: if true; // Cualquiera puede ver las fotos
      allow write: if request.auth != null && request.auth.uid == userId; // Solo el dueño sube
    }
  }
}